package io.github.oiwane.alarmsample.util

class Constants {
    companion object {
        // for ID
        const val EDITED_PROPERTY_ID = "EDITED_PROPERTY_ID"
        const val CHANNEL_ID = "stop_alarm"
        const val ALARM_REQUEST_CODE = "ALARM_REQUEST_CODE"

        // for Log
        const val DATE_FORMAT = "yyyy/MM/dd (E) HH:mm"

        // other
        const val MAP_MAX_SIZE = 10
    }
}